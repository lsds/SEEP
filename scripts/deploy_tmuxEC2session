#!/bin/sh

#current virtual terminal id
counter=$1
#limit virtual terminal id
lastCurrentId=$2
#the file where IPS are stored
FILE=$3

while [ "$counter" -ne "$lastCurrentId" ]
do
	bash exec_tmuxEC2session $counter $lastCurrentId "cd /homes/rc3011/Desktop/development/java/sEEP/scripts"
	let counter=counter-1
done
echo 'Executed change directory to working directory'
#reinitialize variable
#counter=1
#read file again ordered, counter is line number
#while read line
#do
#	COMMAND="bash ec2-deploy.sh $line"
#echo $COMMAND
#	tmux send-keys -t EC2:$counter "$COMMAND" 'Enter'
#	let counter=counter+1
#done <$FILE
##
#echo 'Waiting for all machines to connect'

#counter=$1
#while [ "$counter" -ne 0 ]
#do
#	exec_tmuxEC2session $counter "deployEnvironmentLocal"
#done
